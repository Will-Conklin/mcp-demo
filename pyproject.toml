[project]
name = "frosty-blackwell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.23.1",
    "tinydb>=4.8.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
